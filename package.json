{
  "name": "cadence",
  "version": "0.1.0",
  "private": true,
  "description": "Cadence — habit sequence planner (Capacitor wrap of Babel-in-browser React prototype)",
  "scripts": {
    "start": "npx --yes serve@14 . -l 5173",
    "dev": "npx --yes serve@14 . -l 5173",
    "build": "node build.js",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios",
    "android:run": "npm run sync && npx cap run android",
    "android:apk": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
